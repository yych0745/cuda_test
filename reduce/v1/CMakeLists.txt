find_package(CUDAToolkit REQUIRED)
add_executable(reduce_v1 main.cu)
target_link_libraries(reduce_v1 PRIVATE CUDA::cudart ${CUDA_cublas_LIBRARY})
if(CMAKE_BUILD_TYPE STREQUAL "Debug")
    set_target_properties(reduce_v1 PRIVATE $<$<COMPILE_LANGUAGE:CUDA>:-G>)
endif()